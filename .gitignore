# Runtime IPC + logs (regenerated each run)
.implementations/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# pygbag WASM build output + staging (Phase 7)
build/
wasm_src/

# Playwright MCP browser state (ephemeral, regenerated each session)
.playwright-mcp/

# Runtime high-score file (regenerated by gameplay)
scores.txt

# OS
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Secrets (PixelLab MCP token, etc.) — never commit
.env
.env.local

# Capacitor iPad shell (T112): generated/derived artifacts
ios-app/node_modules/
ios-app/www/
ios-app/ios/App/build/
ios-app/ios/App/App/public/
ios-app/ios/App/App/capacitor.config.json
ios-app/ios/App/App.xcodeproj/xcuserdata/
ios-app/ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/
ios-app/ios/DerivedData/
