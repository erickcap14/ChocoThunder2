# PixelLab MCP API token — get it by signing in at https://www.pixellab.ai
# Copy this file to .env (git-ignored) and fill in your token, then make it
# available to Claude Code's environment before launching (e.g. add to your
# shell profile, or `set -a; source .env; set +a` before `claude`).
PIXELLAB_API_KEY=
